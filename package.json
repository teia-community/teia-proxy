{
  "name": "teia-proxy",
  "version": "0.0.0",
  "devDependencies": {
    "prettier": "^2.7.0",
    "wrangler": "2.0.27"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "format": "prettier --single-quote --tab-width 2 --print-width 140 --write 'src/**/*.@(js|ts|mjs)'"
  }
}
